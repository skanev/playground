> Explain under what circumstances, if any, redundant `DISK-READ` or
> `DISK-WRITE` operations occur during the course of executing a call to
> `B-TREE-INSERT`. (A redundant `DISK-READ` is a `DISK-READ` for a block that is
> already in memory. A redundant `DISK-WRITE` writes to disk a block of
> information that is identical to what is already stored there.)
